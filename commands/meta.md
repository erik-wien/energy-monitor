Switch context to the cross-repo backlog at ~/Git.

Run:
`BACKLOG_CWD=~/Git backlog task list -s "To Do" --plain`

Show all open cross-repo tasks sorted by priority. Ask the user which task to work on, or whether to create a new one. Remind the user that tasks here affect shared libraries (auth, css, map-deploy) or multiple apps simultaneously.
